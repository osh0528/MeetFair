services:
  - type: web
    name: meetfair
    runtime: node
    plan: free
    branch: main
    autoDeployTrigger: commit
    buildCommand: npm ci && npm run build
    startCommand: npm run start --workspace @meetfair/server
    healthCheckPath: /api/health
