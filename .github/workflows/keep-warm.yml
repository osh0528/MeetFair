name: Keep Render Warm

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl --fail -s -o /dev/null -w "%{http_code}\n" https://meetfair-l5ak.onrender.com/api/health
